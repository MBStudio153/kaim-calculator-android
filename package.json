{
  "name": "kaim-calculator",
  "version": "1.0.0",
  "description": "KAIM — Knowledge Asset Impairment Model calculator",
  "private": true,
  "scripts": {
    "build": "echo 'Static site — nothing to build, www/ is the source of truth'",
    "sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
